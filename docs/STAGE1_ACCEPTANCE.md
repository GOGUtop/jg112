# 第一阶段验收标准

第一阶段只验收本地启动链路：

1. APP 可以正常安装；
2. 打开 APP 后不需要输入服务器账号；
3. APP 能启动内置 Node；
4. WebView 能访问 `http://127.0.0.1:8000`；
5. 如果 `assets/sillytavern/server.js` 存在，打开真正的 SillyTavern；
6. 如果没放酒馆，打开 Node 测试页。

不包含：

- 服务器登录；
- 账号 data 下载；
- 聊天记录同步；
- 多账号切换；
- 自动更新；
- 后台保活。
